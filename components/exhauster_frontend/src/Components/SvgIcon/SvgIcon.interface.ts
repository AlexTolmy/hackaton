export enum IconName {
  Calendar = 'calendar',
  ArrowRight = 'arrowRight',
  ArrowLeft = 'arrowLeft',
  Cross = 'cross',
}

export type IconProps = {
  iconName: IconName;
  className?: string;
};

export default {};
